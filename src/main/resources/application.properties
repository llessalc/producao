server.port=8082

spring.application.name=producao

spring.data.mongodb.uri=mongodb://${DB_URL}:27017/producao

# Filas Consumer
events.queue-pedido-producao=${PEDIDOS_PRODUCAO}
events.queue-finaliza-pedido=${PEDIDOS_FINALIZADOS}
spring.cloud.aws.endpoint=${AWS_LOCALHOST}

